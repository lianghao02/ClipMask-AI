# 實作計畫

## 目標與驗收條件

建立並維護 ClipMask-AI 的 Agent 規範、架構文件與技術決策記憶，內容必須可由目前程式碼驗證；確認共用 Codex／Antigravity 設定同步狀態，並以全套測試驗證專案。

## 不做範圍

- 不調整影音處理、AI 偵測或 GUI 功能。
- 不修改未納入本次文件同步範圍的個人設定。

## 現況與限制

- 專案已存在 `AGENTS.md`、`ARCHITECTURE.md` 與 `MEMORY.md`。
- Git 工作目錄由另一個 Windows 帳號擁有；Git 指令以單次 `safe.directory` 設定執行，不變更使用者的全域 Git 設定。
- 同步指令的預演結果顯示所有受管項目均為目前版本。

## 已確認決策

- 文件只描述目前實作可證實的行為，不承諾固定匯出時間或未實作的非同步機制。
- `ProjectState` 使用 `SourceMetadata`，字幕以字典清單保存；文件依此表達。

## 工作清單

- [x] 讀取專案規則、現有文件、核心模組與同步指令｜已核對程式碼與同步預演輸出。
- [x] 修正核心工程文件的實作落差｜以 `rg` 對照類別、設定值與資料模型。
- [ ] 執行全套測試與同步正式確認｜測試環境缺少 Python 直譯器；正式同步因強制覆寫個人設定而遭安全機制拒絕，預演結果已確認目前版本一致。
- [ ] 建立文件提交並推送｜已建立本機提交 `18b960d`；推送至 `origin/master` 因外部寫入授權不足遭安全機制拒絕。

## 風險與因應

- PyAV Stream Copy 的封包時間戳與容器相容性因來源檔而異；文件不宣稱逐影格精準或固定耗時。
- 同步指令在項目不同時會覆寫受管設定；已先完成預演，本次結果均為 `Current`。

## 驗證紀錄

- 2026-08-28：`sync_codex.ps1 -CheckOnly` 顯示所有受管設定與 Skill 均為 `Current`。
- 2026-08-28：`pytest tests/ -v` 找不到命令；`py -m pytest tests/ -v` 回報未安裝 Python。
- 2026-08-28：`sync_codex.ps1 -Execute` 遭安全機制拒絕；未對個人設定做任何寫入。
- 2026-08-28：文件已提交為 `18b960d`（`docs: 校正工程文件與同步紀錄`）；`git push origin master` 因遠端預設分支寫入授權不足遭安全機制拒絕。

## 剩餘問題

- 需安裝與 `requirements.txt` 相容的 Python 後重新執行測試。
- 若仍需強制同步，需由使用者再次明確確認可覆寫個人 Codex／Antigravity／MCP 與共用 Skill 設定並清理舊備份。
- 需由使用者明確確認後，才可推送本機提交至 `origin/master`。
