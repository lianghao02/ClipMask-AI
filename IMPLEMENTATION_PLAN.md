# 實作計畫與發布狀態

## 目標與驗收條件

以真實公務調查與執法影音素材驗證 ClipMask-AI 系統去識別化之核心能力，確認符合 100% 離線隱私邊界，通過完整單元與整合測試，並建置 Windows 64-bit 綠色免安裝版本 (Portable)，達成正式 Stable 發布標準。

## 本輪範圍（2026-09-10）

- 修正編輯後重複 padding 與四邊裁切；保留原模型及偵測靈敏度。
- 增加待檢查清單空間、局部可調高度與選取高亮，不改影音核心及時間軸。
- 使用者確認採「選取後刪除誤抓」流程，避免自動排除低信心人臉。
- 本輪進度、驗證及發布狀態以 `HANDOFF.md` 為準；下列測試與發布敘述為前輪紀錄。

## 前輪現況（歷史紀錄，非本輪驗收）

- **Python 環境**：Python 3.13.15 64-bit 就緒，依賴套件齊全。
- **自動化測試**：20 項測試全數通過（`python -m pytest tests/ -q` 100% PASS）。
- **真實影片實機驗收**：
  - WMV 容器相容性、音訊正規化、VAD 純聲學偵測、繁中字幕磁吸與 SRT 匯出均實測驗證通過。
  - 動態手持攝影與多人物移動之 AI YuNet 人臉追蹤、關鍵影格 Lerp 內插、馬賽克與模糊壓制、快速剪輯均實測驗證通過。
- **Git 狀態**：清除追蹤中 Python Cache（`*.pyc`），working tree 保持整潔。
- **綠色便攜版**：完成 `clipmask.spec` 配置與 PyInstaller 打包，內嵌 YuNet 模型與完整運行環境，免外部安裝與 ffmpeg。

## 已確認決策

- **100% 離線保證**：絕不引入雲端 API 或外部遙測，音訊 VAD 採純聲學 RMS 能量分析，杜絕語音資料外洩。
- **無外部 ffmpeg 依賴**：影音管線純原生採用 PyAV C-binding 封包操作。
- **音訊通道規範化**：輸出容器自動對應標準聲道名稱（mono/stereo/5.1），保證非標記容器之音訊轉碼穩定性。
- **容器 FPS 異常防禦**：針對 ASF/WMV 等特殊 time_base 容器，設有合理的幀率上下限安全回退機制。

## 工作清單

- [x] 讀取專案規則、現有文件與核心架構
- [x] 執行基線自動測試（20 passed）
- [x] 真實影片素材測試與相容性修復（P1 音訊編碼異常、WMV FPS 估算）
- [x] 清理 Git 誤追蹤之 `__pycache__` 檔案
- [x] 建立並驗證 Windows Portable 綠色發布版本
- [x] 修正播放中 Seek 之 PlaybackWorker 與主線程狀態競爭跳動問題（Generation Token 機制）
- [x] 單一視窗 4 種直覺工作模式切換（去識別、聽打、剪輯、完整）
- [x] 文案優化（遮蔽預覽開關、AI 偵測人臉）
- [x] 新增工作模式與 Seek 保護單元測試（22 passed 100%）
- [x] 更新 CHANGELOG.md、version.txt (v1.1.0) 與實作計畫
- [x] 全套回歸測試驗證與 Git Commit / Push

